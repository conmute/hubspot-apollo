type Token {
  refreshToken: String
  accessToken: String
  expiresIn: Int
  hubDomain: String
  scopes: [String]
  hubId: Int
  appId: String
  userId: Int
  email: String
  tokenType: String
  created: Date
  updated: Date
}
